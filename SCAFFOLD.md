# Scaffold trail

Generated by `@bitstone/create-launcher` on 2026-07-05T09:45:59.803Z.
Base launcher: `/Users/yudonghun/MinecraftProjects/blue-zone-launcher`

## Answers
```json
{
  "launcherId": "hardcorerun-launcher",
  "productName": "HardcoreRun Launcher",
  "appId": "hardcorerunlauncher",
  "description": "하코런 (HardcoreRun) 마인크래프트 런처",
  "author": "donghune",
  "homepage": "https://github.com/donghune/hardcorerun-launcher",
  "githubUser": "donghune",
  "githubRepo": "hardcorerun-launcher",
  "copyrightYear": 2026,
  "server": {
    "id": "hardcorerun-main",
    "name": "하코런 서버",
    "description": "하코런 (HardcoreRun) - Fabric 1.21.4",
    "address": "100.89.68.75:25570",
    "minecraftVersion": "1.21.4",
    "icon": "https://raw.githubusercontent.com/donghune/hardcorerun-launcher/master/app/assets/images/SealCircle.png"
  },
  "modulePresets": [
    "fabric-1.21.4",
    "fabric-api-0.119.4+1.21.4",
    "fabric-language-kotlin-1.13.11",
    "sodium-iris"
  ],
  "extraModules": [
    {
      "id": "kr.donghune:hardcorerun_client:1.0-SNAPSHOT",
      "name": "HardcoreRun Mod",
      "type": "FabricMod",
      "artifact": {
        "size": 87865,
        "MD5": "2acee237c879348d7013baf47e4a58a6",
        "url": "https://github.com/donghune/hardcorerun-launcher/releases/download/v1.0.0/hardcorerun_client-1.0-SNAPSHOT.jar"
      }
    }
  ]
}
```

## Next steps
1. Replace branding assets under `app/assets/images/` (logo, SealCircle.png) so they match `HardcoreRun Launcher`.
2. Review `distribution.json` — mod jars referenced via GitHub Releases need real `size`/`MD5`.
3. Inspect the residuals listed below and edit by hand where needed.
4. `npm install && npm start` to dev-launch.

## Residuals from the base launcher (review by hand)

- `app/assets/lang/_custom.toml` — Blue Zone (literal)
- `app/assets/lang/_custom.toml` — BLUE ZONE (uppercase)
- `app/assets/lang/ko_KR.toml` — Blue Zone (literal)

Notes:
- Game-specific lines (e.g. `welcomeHeader`, `welcomeDescription`, gameplay strings in `app/assets/lang/*.toml`) are intentionally left intact — they describe the *event*, not the launcher, so they need a manual rewrite for each new event.
- Shader/file URLs in `distribution.json` still point at the base launcher's GitHub Release. After uploading the same assets to `donghune/hardcorerun-launcher` releases, update those URLs.
